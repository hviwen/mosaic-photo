## 优化调整

### 涉及代码（包括但不限于）
- src/components/CanvasStage.vue

### 交互优化
1. 左侧和右侧折叠后，左侧和右侧操作区的内容应该收起为图标，而不是将操作区的内容压缩
2. Canvas画布应该默认铺满中间视觉区域
3. 当上传图片后，点击左侧切换画布尺寸时，应该在切换后自动重新布局，而不是保持原布局
4. 选中调整某个图片后，再点击左侧切换画布尺寸时，应该在切换后自动重新布局，而不是保持原布局
5. 当调整画布尺寸时，如果右侧操作区未折叠，应该将右侧操作区也一起调整大小