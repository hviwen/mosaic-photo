请修复以下问题并完成优化调整：

## 一、问题修复（按优先级排序）

### P0-1：单张照片裁剪模式 - 显示完整原图而非已裁剪区域

**当前问题：**
- 用户选中单张照片进入裁剪模式时，显示的是已经被智能裁剪后的区域（`photo.crop` 或 `photo.layoutCrop`）
- 用户只能在已裁剪的范围内进一步调整，无法看到完整原图

**期望行为：**
- 用户选中单张照片进入裁剪模式时，应重置 `crop` 为完整原图尺寸：`{ x: 0, y: 0, width: photo.imageWidth, height: photo.imageHeight }`
- 在画布中保持照片的显示位置和大小不变（`cx`、`cy`、`scale` 不变）
- 用户通过拖动照片内容来调整裁剪区域（类似"移动照片内容，裁剪框固定"的交互）
- 点击"应用"后，保存新的 `crop` 值，并触发重新排版

**涉及文件：**
- `src/stores/mosaic.ts` - `enterCropMode()` 函数：进入裁剪模式时重置 crop 为全图
- `src/components/CanvasStage.vue` - `drawPhoto()` 函数：裁剪模式下绘制完整原图
- `src/components/PhotoEditor.vue` - 裁剪交互逻辑：确保用户可以拖动照片内容

---

### P0-2：照片导入数量限制 - 支持最多 150 张

**当前问题：**
- 导入超过 81 张照片后，后续照片不显示在画布中（可能是布局算法或渲染限制）

**期望行为：**
- 支持导入最多 150 张照片
- 当用户尝试导入超过 150 张时，显示提示："最多支持导入 150 张照片，已自动选择前 150 张"
- 确保 150 张照片能正常排版和渲染（可能需要优化 `fillArrangePhotos` 或渲染性能）

**涉及文件：**
- `src/stores/mosaic.ts` - `addPhotos()` 函数：添加数量检查和提示
- `src/composables/useLayout.ts` - `fillArrangePhotos()` 函数：确保支持 150 张照片的布局
- `src/components/CanvasStage.vue` - 渲染逻辑：确保大量照片时性能可接受

---

### P1-3：原图缩略模块 - 显示人脸检测红框

**当前问题：**
- 右侧编辑栏的"原图缩略"模块仅显示缩略图，未显示人脸检测结果

**期望行为：**
- 当用户选中单张照片后，在"原图缩略"模块的缩略图上叠加显示人脸检测框
- 使用红色矩形框（`stroke: 'red'`, `lineWidth: 2`）标注所有检测到的人脸位置
- 检测框坐标需要按缩略图尺寸进行缩放映射

**实现步骤：**
1. 在 `src/components/PhotoEditor.vue` 中定位"原图缩略"模块的渲染代码
2. 调用 `getSmartDetections(photoId)` 获取检测结果
3. 筛选 `kind === 'face'` 的检测框
4. 在缩略图 canvas 上绘制红色矩形框（注意坐标缩放比例）

**涉及文件：**
- `src/components/PhotoEditor.vue` - "原图缩略"模块渲染逻辑
- `src/utils/smartCrop.ts` - `getSmartDetections()` 函数（已存在）

---

## 二、优化调整

### 新增画布尺寸预设

在 `src/stores/mosaic.ts` 或画布尺寸配置文件中，新增以下预设（假设 300 DPI）：

| 尺寸（cm）    | 像素尺寸（300 DPI）      |
|--------------|------------------------|
| 50×40        | 5906×4724 px           |
| 60×40        | 7087×4724 px           |
| 70×50        | 8268×5906 px           |
| 80×60        | 9449×7087 px           |
| 90×60        | 10630×7087 px          |
| 100×70       | 11811×8268 px          |
| 100×80       | 11811×9449 px          |

**涉及文件：**
- `src/stores/mosaic.ts` 或画布预设配置相关文件
- UI 组件中的尺寸选择器

---

## 三、实现要求

1. **禁止使用 `any` 类型**，所有新增代码必须有明确的 TypeScript 类型注解
2. **最小化改动**，仅修改必要的代码，避免影响其他功能
3. **添加中文注释**，说明关键逻辑和修改原因
4. **保持代码风格一致**，遵循项目现有的 Vue 3 Composition API + TypeScript 规范
5. **测试验证**：
   - P0-1：选中照片进入裁剪模式，确认显示完整原图
   - P0-2：导入 100 张、150 张、160 张照片，验证限制和提示
   - P1-3：选中有人脸的照片，确认缩略图上显示红框
   - 优化：确认新增尺寸预设可正常选择和使用