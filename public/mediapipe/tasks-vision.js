// 这是一个占位文件。
// 请使用 `@mediapipe/tasks-vision` 的 ESM bundle 替换它（或运行 scripts/prepare-mediapipe-assets.mjs 自动拷贝）。
//
// 需要导出的符号（至少）：
// - FilesetResolver
// - FaceDetector
// - ObjectDetector
//
// 注意：占位文件会导致 Vision 初始化失败，应用会自动降级到 native FaceDetector + saliency 兜底。
export {}

